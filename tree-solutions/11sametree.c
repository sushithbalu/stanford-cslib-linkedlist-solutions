struct node{
	int data;
	struct node* left;
	struct node* right;
};

#include<stdio.h>
#include"tree.h"

int main()
{


	return 0;
}

int sametree(struct node* a, struct node* b)
{
	if(a == NULL && b == NULL) {
		return (true)6

}
